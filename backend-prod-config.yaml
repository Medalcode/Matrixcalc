# Este archivo ha sido saneado por razones de seguridad.
# Los secretos (como DATABASE_URL o ALLOWED_HOSTS) no deben ser almacenados en texto plano en el repositorio.
# Utiliza variables de entorno o un gestor de secretos (ej. AWS Secrets Manager, Google Secret Manager).
